---
description: Build project and verify no errors
allowed-tools: Bash(pnpm:*), Read
---

Run the following verification steps:

1. Run `pnpm build` and capture any errors
2. Run `pnpm lint` and capture any warnings/errors
3. If any issues found, report them clearly
4. If all passes, confirm the build is clean

Do not attempt to fix issues automatically - just report findings.
